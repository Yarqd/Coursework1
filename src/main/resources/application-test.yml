spring:
  datasource:
    url: jdbc:h2:mem:coursework;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

storage:
  minio:
    endpoint: http://localhost:9000
    public-endpoint: http://localhost:9000
    access-key: test
    secret-key: test-secret
    bucket: coursework-media-test
